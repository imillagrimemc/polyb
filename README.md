# PolyB — Modular Polybus-Based CRM & Call Automation System

**PolyB** — это модульная система интеграции CRM и оркестрации звонков, построенная поверх шины сообщений **Polybus**.
Проект состоит из двух основных сервисов:

* **CRM Connector** — взаимодействует с CRM (создание и поиск контактов, лидов).
* **Call Orchestrator** — получает данные из CRM и инициирует исходящий звонок через внешний VoIP-сервис.

Проект легко расширяется, так как каждый компонент — отдельный сервис Polybus.

---

## 📂 Структура проекта

```
PolyB/
│
├── crm_connector.py      # Сервис интеграции c CRM
├── api_client.py         # Низкоуровневый клиент CRM API
└── call_orchestrator.py  # Оркестратор звонков
```

---

## 🔌 Архитектура

### 1) CRM Connector (порт 9103)

Отвечает за:

* Поиск контакта в CRM по номеру телефона.
* Создание контакта, если он отсутствует.
* Создание лида (Lead) в CRM.
* Возврат информации в Polybus-шину.

**Методы Polybus**:
`push_to_crm(params)`

---

### 2) Call Orchestrator (порт 9104)

После получения результата от CRM Connector:

* Разбирает вложенный JSON.
* Извлекает необходимые данные: `leadId`, `phoneNumber`, `createdById`.
* Вызывает внешний API телефонии (`http://192.168.1.100:5000/call`).
* Запускает процесс инициализации звонка.

**Методы Polybus**:
`start_call(params)`

---

### 3) Связь между сервисами

```
[ External App ]
        |
        v
   push_to_crm
        |
        v
[ CRM Connector ] ----> CRM
        |
        v
   start_call
        |
        v
[ Call Orchestrator ] ----> VoIP API
```

---

## 🚀 Запуск сервисов

### Установка зависимостей

```bash
pip install -r requirements.txt
```

(Если файла `requirements.txt` нет — могу сгенерировать.)

---

### Запуск CRM Connector

```bash
python crm_connector.py
```

По умолчанию сервис поднимает Polybus-сервер:

```
name: crm_connector
host: 127.0.0.1
port: 9103
methods: ["push_to_crm"]
```

---

### Запуск Call Orchestrator

```bash
python call_orchestrator.py
```

Polybus-сервер:

```
name: call_orchestrator
host: 127.0.0.1
port: 9104
methods: ["start_call"]
```

---

## 📡 Как работает передача данных

### Пример входных параметров для CRM Connector:

```json
{
  "firstName": "Иван",
  "lastName": "Иванов",
  "phoneNumber": "+79995553322"
}
```

### CRM Connector выполняет:

1. Поиск контакта по номеру.
2. Если нет — создаёт контакт и лида.
3. Возвращает объект:

```json
{
  "status": "pushed",
  "crm_id": {
    "status": "success",
    "resData": {
      "id": "65021a1e47d57e2ac",
      "phoneNumber": "79995553322",
      "createdById": "1"
    }
  }
}
```

---

## 📞 Инициализация звонка

Call Orchestrator преобразует JSON в плоский вид и вызывает:

```
POST http://192.168.1.100:5000/call
Content-Type: application/json
{
  "phone": "5553322"
}
```

(берётся последние 9 цифр номера)

---

## ⚙️ Настройка интеграции с CRM

В `CrmClient()` задаются:

```python
API('http://192.168.1.100:8080', 'API_KEY')
```

Можно вынести в `.env` или config.json — могу добавить по запросу.

---

## 🧰 Используемые технологии

* **Polybus** — внутренняя сервисная шина.
* **Python 3.10+**
* **Requests** — HTTP клиент.
* **CRM API**
* **REST-интеграция с телеком-сервером**

---

## 📖 Дополнительно

* Логику ретраев + fault tolerance.
* Автоматическую документацию методов Polybus.
* Диаграмму последовательности (PlantUML).
* CI/CD + GitHub Actions.

---

## 📜 Лицензия

MIT License.
